1. Signup

User signs up with Supabase → gets a user.id.

Backend stores user in your users table.

At this point, the user does not yet have a school.

2. Register School

After signup, user goes to the RegisterSchool page.

You send user.id + school details to backend → insert into schools table with user_id as foreign key.

Backend responds with the school object.

In frontend, you store both user + school in Context.

3. Login

On login, Supabase gives you the user.id.

You fetch both the user record and the school record from your backend.

Store them in Context so the whole app can use them (e.g., dashboard, classes, teachers).

4. Dashboard

Context provides user + school.

Pages like Students, Teachers, Classes query the backend filtered by school_id.

Example: GET /api/students?school_id={school.id}











SCHOOL TABLE SCHEMA

-- CREATE TABLE schools (
--     id UUID PRIMARY KEY DEFAULT gen_random_uuid(),   -- Auto-generate UUID for school
--     name VARCHAR(255) UNIQUE,
--     logo TEXT,
--     email VARCHAR(255) UNIQUE NOT NULL,
--     address VARCHAR(255),
--     website VARCHAR(255) UNIQUE,
--     user_id UUID NOT NULL,   -- Each school belongs to exactly one user
--     created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
--     updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
--     CONSTRAINT fk_user FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE
-- );

  SCHOOL SESSION SCHEMA

CREATE TABLE school_sessions (
  session_id UUID PRIMARY KEY DEFAULT gen_random_uuid(),       -- unique ID for each session
  session_name VARCHAR(100) NOT NULL,                          -- e.g., "2024 / 2025 Academic Year"
  term VARCHAR(20) NOT NULL CHECK (term IN ('first', 'second', 'third')),  -- restricted term options
  start_date DATE NOT NULL,
  end_date DATE NOT NULL,
  status VARCHAR(20) DEFAULT 'upcoming' CHECK (status IN ('upcoming', 'active', 'completed')),  -- session state
  created_by UUID NOT NULL,                                    -- user/admin who created the session
  school_id UUID NOT NULL,                                     -- link to the school
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,              -- creation timestamp
  
  CONSTRAINT fk_school FOREIGN KEY (school_id)
    REFERENCES schools(id) ON DELETE CASCADE,

  CONSTRAINT fk_created_by FOREIGN KEY (created_by)
    REFERENCES users(id) ON DELETE SET NULL,

  CONSTRAINT session_unique UNIQUE (school_id, session_name, term)  -- prevent duplicates
);